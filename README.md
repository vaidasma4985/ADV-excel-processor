# ADV-excel-processor

## Folder layout

- `app.py`: main Streamlit entry point that routes to both tools.
- `wire_tool/`: wire sizing tool package and UI.
- `component_correction/`: component correction processor and its requirements file.
- `pin_templates.json`: shared template storage used by the wire tool.
